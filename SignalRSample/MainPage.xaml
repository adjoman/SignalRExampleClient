<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SignalRSample.MainPage">

    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">

            <Label
                x:Name="chatMessages"
                Text="Start Here"
                FontSize="18"
                TextColor="Black"
                HorizontalOptions="Center" />

            <Entry
                x:Name="myChatMessage"
                WidthRequest="200"
                Text=""
                FontSize="18"
                HorizontalOptions="Center" />

            <Button
                x:Name="sendButton"
                Text="Send Message"
                Clicked="OnCounterClicked"
                HorizontalOptions="Center" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>

